{
  "name": "@rari/monorepo",
  "type": "module",
  "private": true,
  "packageManager": "pnpm@12.1.0",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "clean": "pnpm -r run clean",
    "fmt": "vp fmt",
    "fmt:check": "vp fmt --check",
    "lint": "pnpm fmt:check && pnpm -r run lint && vp lint crates/ && eslint crates/ packages/ web/ examples/ test/ package.json pnpm-workspace.yaml",
    "lint:fix": "pnpm fmt && pnpm -r run lint:fix && vp lint crates/ --fix && eslint crates/ packages/ web/ examples/ test/ package.json pnpm-workspace.yaml --fix",
    "lint:knip": "knip",
    "test:unit": "vp test",
    "test:unit:run": "vp test run",
    "test:unit:coverage": "vp test run --coverage",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:smoke": "playwright test smoke.spec.ts"
  },
  "devDependencies": {
    "@napi-rs/cli": "catalog:build",
    "@playwright/test": "catalog:testing",
    "@rari/lint": "workspace:*",
    "@types/node": "catalog:typescript",
    "@types/react": "catalog:react",
    "@vitest/coverage-v8": "catalog:testing",
    "eslint": "catalog:eslint",
    "knip": "catalog:eslint",
    "react": "catalog:react",
    "typescript": "catalog:typescript",
    "vite": "catalog:build",
    "vite-plus": "catalog:build",
    "vitest": "catalog:build"
  }
}
